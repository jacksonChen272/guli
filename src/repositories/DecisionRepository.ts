import { formulaVersion } from '../config/decisionFormula'
import { decisionEngine } from '../services/decision/DecisionEngine'
import { DecisionComparisonService } from '../services/decision/DecisionComparisonService'
import { calculateStockHealth } from '../services/stockHealthService'
import { useWatchlistStore } from '../stores/watchlistStore'
import type { DecisionComparison, DecisionEntityType, DecisionResult } from '../types/decision'
import { IndustrySnapshotRepository } from './IndustrySnapshotRepository'
import { SnapshotRepository } from './SnapshotRepository'
import { StockSnapshotRepository } from './StockSnapshotRepository'
import type { StockRepository } from './StockRepository'
export class DecisionRepository {private cache=new Map<string,Promise<DecisionResult>>();private comparison=new DecisionComparisonService();constructor(private readonly markets=new SnapshotRepository(),private readonly industries=new IndustrySnapshotRepository(),private readonly stocks=new StockSnapshotRepository(),private readonly stockData?:StockRepository){}private cached(key:string,producer:()=>Promise<DecisionResult>){const full=`${formulaVersion}:${key}`;const hit=this.cache.get(full);if(hit)return hit;const value=producer().catch((error)=>{this.cache.delete(full);throw error});this.cache.set(full,value);return value}getMarketDecision(date?:string){return this.cached(`market:${date??'latest'}`,async()=>decisionEngine.market(date?await this.markets.getByDate(date):await this.markets.getLatest()))}getIndustryDecision(industryId:string,date?:string){return this.cached(`industry:${industryId}:${date??'latest'}`,async()=>{const snapshot=date?await this.industries.getByDate(date):await this.industries.getLatest();const item=snapshot.industries.find((entry)=>entry.industryId===industryId);if(!item)throw new Error(`找不到產業決策資料：${industryId}`);return decisionEngine.industry(item,snapshot.tradeDate,snapshot.warnings)})}getStockDecision(symbol:string,date?:string){return this.cached(`stock:${symbol}:${date??'latest'}`,async()=>{const snapshot=await this.stocks.getBySymbol(symbol,date);const market=await this.getMarketDecision(snapshot.tradeDate).catch(()=>null);const universe=this.stockData?.getSnapshot()??[];const mockStock=universe.find((item)=>item.symbol===symbol);const health=mockStock?calculateStockHealth(mockStock,universe):null;let industry:DecisionResult|null=null;if(mockStock){const industrySnapshot=await this.industries.getByDate(snapshot.tradeDate).catch(()=>null);const matched=industrySnapshot?.industries.find((item)=>item.industryName===mockStock.industry);if(matched)industry=decisionEngine.industry(matched,industrySnapshot!.tradeDate,industrySnapshot!.warnings)}return decisionEngine.stock(snapshot,health,market,industry)})}getWatchlistDecision(){return this.cached('watchlist:current',async()=>{const symbols=useWatchlistStore.getState().items.map((item)=>item.symbol);const decisions=await Promise.all(symbols.map((symbol)=>this.getStockDecision(symbol).catch(()=>null)));return decisionEngine.watchlist(decisions.filter((item):item is DecisionResult=>item!==null))})}async getDecisionComparison(entityType:DecisionEntityType,entityId:string,currentDate:string,previousDate?:string):Promise<DecisionComparison>{const dates=entityType==='industry'?await this.industries.getHistory(50).then((rows)=>rows.map((row)=>row.tradeDate)):entityType==='stock'?await this.stocks.getAvailableDates():await this.markets.getAvailableDates();const previous=previousDate??dates.find((date)=>date<currentDate);const get=(date:string)=>entityType==='market'?this.getMarketDecision(date):entityType==='industry'?this.getIndustryDecision(entityId,date):entityType==='stock'?this.getStockDecision(entityId,date):this.getWatchlistDecision();const current=await get(currentDate);const prior=previous?await get(previous).catch(()=>undefined):undefined;return this.comparison.compare(current,prior)}clearCache(){this.cache.clear()}}
