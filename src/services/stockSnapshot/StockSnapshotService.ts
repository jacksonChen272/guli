import { StockSnapshotRepository } from '../../repositories/StockSnapshotRepository'
export class StockSnapshotService { constructor(private readonly repository=new StockSnapshotRepository()){}getLatestDataset(){return this.repository.getLatestIndex()}getSnapshot(symbol:string,date?:string){return this.repository.getBySymbol(symbol,date)}getHistory(symbol:string,days:number){return this.repository.getHistory(symbol,days)}getDiff(symbol:string,currentDate?:string,previousDate?:string){return this.repository.getDiff(symbol,currentDate,previousDate)}getMemory(symbol:string,days:number){return this.repository.getMemory(symbol,days)}getAvailableDates(){return this.repository.getAvailableDates()}clearCache(){this.repository.clearCache()} }
