import type { StockSnapshotDiff, StockSnapshotItem } from '../../types/stockSnapshot'
const delta=(current:number|null,previous:number|null)=>current===null||previous===null?null:Math.round((current-previous)*100)/100
export class StockSnapshotDiffService { compare(current:StockSnapshotItem,previous?:StockSnapshotItem):StockSnapshotDiff { if(!previous)return{symbol:current.symbol,currentDate:current.tradeDate,previousDate:null,available:false,changes:{close:null,changePercent:null,tradeValue:null,peRatio:null,dailyRangePercent:null,snapshotScore:null,riskCount:null},addedRisks:[],removedRisks:[],warnings:['找不到前一個可用交易日快照，無法產生比較。']};const currentRisks=new Set(current.risks.map((item)=>item.code));const previousRisks=new Set(previous.risks.map((item)=>item.code));return{symbol:current.symbol,currentDate:current.tradeDate,previousDate:previous.tradeDate,available:true,changes:{close:delta(current.quote.close,previous.quote.close),changePercent:delta(current.quote.changePercent,previous.quote.changePercent),tradeValue:delta(current.quote.tradeValue,previous.quote.tradeValue),peRatio:delta(current.quote.peRatio,previous.quote.peRatio),dailyRangePercent:delta(current.dailyRangePercent,previous.dailyRangePercent),snapshotScore:delta(current.snapshotScore,previous.snapshotScore),riskCount:current.risks.length-previous.risks.length},addedRisks:[...currentRisks].filter((code)=>!previousRisks.has(code)),removedRisks:[...previousRisks].filter((code)=>!currentRisks.has(code)),warnings:[]}} }
