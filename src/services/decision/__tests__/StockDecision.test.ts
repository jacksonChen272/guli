import { describe,expect,it } from 'vitest'
import { decisionEngine } from '../DecisionEngine'
import { stockSnapshot } from './decisionTestFixtures'
describe('stock decision',()=>{it('returns 0-100 score',()=>{const result=decisionEngine.stock(stockSnapshot(),null,null,null);expect(result.score).toBeGreaterThanOrEqual(0);expect(result.score).toBeLessThanOrEqual(100)});it('keeps stock snapshot score separate',()=>{const snapshot=stockSnapshot({snapshotScore:99,priceStrengthScore:60,liquidityScore:60});expect(decisionEngine.stock(snapshot,null,null,null).score).not.toBe(99)});it('normalizes missing industry and health factors',()=>expect(decisionEngine.stock(stockSnapshot(),null,null,null).trace.normalizationApplied).toBe(true));it('high risk lowers risk-control factor',()=>{const safe=decisionEngine.stock(stockSnapshot(),null,null,null);const risky=decisionEngine.stock(stockSnapshot({risks:[{code:'wide_daily_range',severity:'high',title:'x',reason:'x',source:'derived'}]}),null,null,null);expect(risky.factors.find((item)=>item.code==='risk_control')!.normalizedScore).toBeLessThan(safe.factors.find((item)=>item.code==='risk_control')!.normalizedScore!)});it('keeps official source evidence',()=>expect(decisionEngine.stock(stockSnapshot(),null,null,null).sources.some((source)=>source.type==='official')).toBe(true))})
