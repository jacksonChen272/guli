import type { DataCoverageReport } from '../types/dataCoverage'
export interface CoverageInput { marketAvailable: boolean; stockRecords: number; targetStocks: number; institutionalRecords: number; institutionalTarget: number; industryOfficialRecords: number; industryTarget: number; historyDays: number; updatedAt: string | null; stale: boolean }
const percent = (value: number, target: number) => target <= 0 ? 0 : Math.max(0, Math.min(100, Math.round(value / target * 100)))
export function calculateDataCoverage(input: CoverageInput): DataCoverageReport { const institutional = percent(input.institutionalRecords, input.institutionalTarget); return { marketOfficialPercent: input.marketAvailable ? 100 : 0, stockOfficialPercent: percent(input.stockRecords, input.targetStocks), institutionalOfficialPercent: institutional, industryOfficialPercent: percent(input.industryOfficialRecords, input.industryTarget), historyDays: Math.max(0, input.historyDays), updatedAt: input.updatedAt, stale: input.stale, mockFields: ['技術歷史', '健康分數中的法人歷史', '產業分類與產業資金流', '市場溫度部分因子'], derivedFields: ['Decision Score', 'Confidence', 'Snapshot Score', '健康分數', '漲幅 ≥ 9.5%／跌幅 ≤ -9.5% 家數'], missingFields: [...(institutional ? [] : ['官方法人資料']), '官方產業分類與產業歷史'] } }
