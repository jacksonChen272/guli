import { ArrowDownRight, ArrowUpRight, Minus } from 'lucide-react'
import type { HealthFactor } from '../../types/insight'

export function FactorScoreList({ factors }: { factors: HealthFactor[] }) {
  return <div className="space-y-5">{factors.map((factor) => { const delta = factor.score - factor.industryAverage; const Icon = delta > 2 ? ArrowUpRight : delta < -2 ? ArrowDownRight : Minus; return <div key={factor.key}><div className="flex items-start justify-between gap-4"><div><p className="text-[15px] font-medium text-slate-100">{factor.label} <span className="ml-1 text-xs font-normal text-slate-500">權重 {Math.round(factor.weight * 100)}%</span></p><p className="mt-1 text-sm leading-6 text-slate-400">{factor.explanation}</p></div><div className="shrink-0 text-right"><span className="mono text-2xl text-white">{factor.score}</span><span className={`ml-2 inline-flex items-center text-xs ${delta >= 0 ? 'text-red-300' : 'text-emerald-300'}`}><Icon size={14} />{Math.abs(delta)}</span></div></div><div className="mt-3 h-2 overflow-hidden rounded-full bg-white/5"><div className="h-full rounded-full bg-brand-400 transition-[width] duration-300" style={{ width: `${factor.score}%` }} /></div></div> })}</div>
}
