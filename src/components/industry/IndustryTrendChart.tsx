const points = (values: number[]) => { if (!values.length) return ''; const min = Math.min(...values); const max = Math.max(...values); return values.map((value, index) => `${index / Math.max(1, values.length - 1) * 100},${42 - (value - min) / Math.max(1, max - min) * 34}`).join(' ') }
export function IndustryTrendChart({ values, label = '強度趨勢' }: { values: number[]; label?: string }) { return <div aria-label={label} className="overflow-hidden rounded-xl border border-white/[.06] bg-black/10 p-3"><svg viewBox="0 0 100 46" className="h-28 w-full" role="img"><title>{label}</title><line x1="0" y1="42" x2="100" y2="42" stroke="rgba(148,163,184,.12)"/><polyline fill="none" stroke="#53d9b2" strokeWidth="1.7" vectorEffect="non-scaling-stroke" points={points(values)}/></svg></div> }
