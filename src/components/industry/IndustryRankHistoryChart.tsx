export function IndustryRankHistoryChart({ ranks }: { ranks: Array<{ date: string; rank: number }> }) { return <div className="space-y-2">{ranks.map((item) => <div key={item.date} className="grid grid-cols-[82px_1fr_34px] items-center gap-3 text-[10px]"><span className="mono text-slate-600">{item.date.slice(5)}</span><div className="h-1.5 overflow-hidden rounded-full bg-white/[.05]"><div className="h-full rounded-full bg-blue-400/70" style={{ width: `${Math.max(8, 100 - item.rank * 7)}%` }}/></div><span className="mono text-right text-slate-300">#{item.rank}</span></div>)}</div> }
