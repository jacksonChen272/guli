import type { IndustryStockSummary } from '../../types/industrySnapshot'
export function IndustryLeaderList({ title, stocks }: { title: string; stocks: IndustryStockSummary[] }) { return <div><p className="mb-3 text-xs font-medium text-slate-300">{title}</p><div className="space-y-2">{stocks.length ? stocks.map((stock) => <div key={stock.symbol} className="flex items-center justify-between rounded-xl border border-white/[.05] bg-white/[.015] px-3 py-2.5"><div><p className="text-xs text-white">{stock.name} <span className="mono text-[9px] text-slate-600">{stock.symbol}</span></p><p className="mt-1 text-[9px] text-slate-600">健康分數 {stock.healthScore ?? '—'}</p></div><span className={`mono text-xs ${(stock.changePercent ?? 0) > 0 ? 'text-red-300' : (stock.changePercent ?? 0) < 0 ? 'text-emerald-300' : 'text-slate-400'}`}>{stock.changePercent === null ? '—' : `${stock.changePercent > 0 ? '+' : ''}${stock.changePercent.toFixed(2)}%`}</span></div>) : <p className="text-[10px] text-slate-600">目前沒有足夠成分股資料。</p>}</div></div> }
