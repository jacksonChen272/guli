export type TabOption<T extends string | number> = { value: T; label: string }
export function Tabs<T extends string | number>({ options, value, onChange, className = '' }: { options: TabOption<T>[]; value: T; onChange: (value: T) => void; className?: string }) {
  const move = (direction: number) => { const current = options.findIndex((option) => option.value === value); const next = (current + direction + options.length) % options.length; onChange(options[next].value) }
  return <div className={`inline-flex rounded-xl border border-white/[0.07] bg-black/20 p-1 ${className}`} role="tablist" onKeyDown={(event) => { if (event.key === 'ArrowRight' || event.key === 'ArrowDown') { event.preventDefault(); move(1) } if (event.key === 'ArrowLeft' || event.key === 'ArrowUp') { event.preventDefault(); move(-1) } }}>{options.map((option) => <button type="button" role="tab" aria-selected={value === option.value} tabIndex={value === option.value ? 0 : -1} key={option.value} onClick={() => onChange(option.value)} className={`min-h-10 rounded-lg px-3 py-2 text-[11px] transition ${value === option.value ? 'bg-white/[0.08] text-white shadow-sm' : 'text-slate-500 hover:text-slate-300'}`}>{option.label}</button>)}</div>
}
