import type { DecisionSource } from '../../types/decision'
import { Badge } from '../ui/Badge'
export function DecisionSourceList({sources,warnings=[]}:{sources:DecisionSource[];warnings?:string[]}){return <div className="space-y-2">{sources.map((source)=><div key={source.id} className="flex min-h-12 items-center justify-between gap-3 rounded-xl border border-white/[.06] px-3"><div><p className="text-[11px] text-slate-300">{source.name}</p><p className="mt-1 text-[8px] text-slate-600">{source.fields.join(' · ')} {source.tradeDate?`· ${source.tradeDate}`:''}</p></div><Badge tone={source.type==='official'?'info':source.type==='missing'?'warning':'neutral'}>{source.type}</Badge></div>)}{warnings.map((warning)=><p key={warning} className="rounded-lg bg-amber-400/[.03] px-3 py-2 text-[9px] leading-4 text-amber-200/75">{warning}</p>)}</div>}
