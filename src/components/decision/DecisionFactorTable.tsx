import type { DecisionFactor } from '../../types/decision'
import { Badge } from '../ui/Badge'
const show=(value:number|null)=>value===null?'—':value.toFixed(1)
export function DecisionFactorTable({factors}:{factors:DecisionFactor[]}){return <div className="overflow-hidden rounded-xl border border-white/[.06]"><div className="hidden grid-cols-[1.3fr_.7fr_.55fr_.7fr_.7fr] gap-3 border-b border-white/[.06] px-4 py-2 text-[9px] text-slate-600 sm:grid"><span>因子</span><span>分數</span><span>權重</span><span>貢獻</span><span>來源</span></div>{factors.map((factor)=><div key={factor.code} className="grid min-h-16 grid-cols-[1fr_auto] items-center gap-3 border-b border-white/[.05] px-4 py-3 last:border-0 sm:grid-cols-[1.3fr_.7fr_.55fr_.7fr_.7fr]"><div><p className="text-xs text-white">{factor.name}</p><p className="mt-1 text-[9px] leading-4 text-slate-600">{factor.explanation}</p></div><span className="mono text-xs text-slate-200">{show(factor.normalizedScore)}</span><span className="hidden text-[10px] text-slate-500 sm:block">{(factor.weight*100).toFixed(0)}%</span><span className={`mono hidden text-[10px] sm:block ${(factor.contribution??0)>=0?'text-red-300':'text-emerald-300'}`}>{factor.contribution===null?'—':`${factor.contribution>0?'+':''}${factor.contribution.toFixed(2)}`}</span><span className="hidden sm:block"><Badge tone={factor.sourceType==='official'?'info':factor.sourceType==='missing'?'warning':'neutral'}>{factor.sourceType}</Badge></span></div>)}</div>}
