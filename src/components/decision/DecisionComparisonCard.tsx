import type { DecisionComparison } from '../../types/decision'
import { Card } from '../ui/Card'
export function DecisionComparisonCard({comparison}:{comparison:DecisionComparison|null}){return <Card title="與前次決策比較" eyebrow="DECISION DIFF"><div className="p-5">{!comparison||!comparison.available?<p className="text-xs text-slate-500">{comparison?.warnings[0]??'尚未讀取比較資料。'}</p>:<div className="grid grid-cols-2 gap-3 sm:grid-cols-4"><Metric label="前次日期" value={comparison.previousDate??'—'}/><Metric label="分數變化" value={comparison.scoreChange===null?'—':`${comparison.scoreChange>0?'+':''}${comparison.scoreChange.toFixed(1)}`}/><Metric label="信心變化" value={`${comparison.confidenceChange!>0?'+':''}${comparison.confidenceChange}`}/><Metric label="標籤變更" value={comparison.labelChanged?'是':'否'}/></div>}</div></Card>}
function Metric({label,value}:{label:string;value:string}){return <div className="rounded-xl border border-white/[.06] p-3"><p className="text-[9px] text-slate-600">{label}</p><p className="mono mt-2 text-xs text-white">{value}</p></div>}
