import type { SnapshotIndexItem } from '../../types/snapshot'
export function SnapshotTimeline({ items, selected, onSelect }: { items: SnapshotIndexItem[]; selected?: string; onSelect: (date: string) => void }) { return <div className="flex gap-2 overflow-x-auto pb-2 md:flex-col md:overflow-visible">{items.map((item) => <button type="button" key={item.tradeDate} onClick={() => onSelect(item.tradeDate)} className={`min-h-14 min-w-40 rounded-xl border px-4 py-3 text-left transition md:min-w-0 ${selected === item.tradeDate ? 'border-brand-400/30 bg-brand-400/[.06]' : 'border-[var(--border-subtle)] bg-white/[.015] hover:border-white/10'}`}><p className="mono text-[10px] text-slate-300">{item.tradeDate}</p><p className="mt-1 text-[9px] text-slate-600">{item.marketStatus} · {item.marketTemperature} 分</p></button>)}</div> }
